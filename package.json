{
  "name": "material-evidence",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "engines": { "node": ">=20.9" },
  "scripts": {
    "ask": "pnpm --filter @me/engine ask",
    "test": "pnpm --filter @me/engine test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @me/web dev",
    "build": "pnpm -r build",
    "extract": "python3 scripts/extract_features.py"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
