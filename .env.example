# Optional. With a key, Gemini narrates the engine's findings in nicer prose and
# the agent can inspect evidence / find better alternatives. WITHOUT it, the whole
# app still works (deterministic parser + templated prose) — facts never change.
GEMINI_API_KEY=
# Optional model override (default: gemini-2.5-flash)
# ME_MODEL=
